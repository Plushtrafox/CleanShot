using UnityEngine;
using UnityEngine.UI;
using TMPro;

public class PoderManager : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    //controlar el slider de cada poder usando el input cuando cambia y cuando se usa
    //primero parte funcional, colocal el cooldown luego de cada uso con una  variable que se apague luego de uso y un
    //invoke que lo resetee luego de un tiempo
    //segundo referenciarlo a los sliders 
    //fibalmente referenciar al slider del poder actual fuera del menu de armas

    // Update is called once per frame
    void Update()
    {
        
    }
}
